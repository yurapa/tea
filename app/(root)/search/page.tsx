// import { getAllProducts } from "@/lib/actions/product.actions";
// import ProductList from "@/components/shared/product/product-list";

const SearchPage = async () => {
  // const latestProducts = await getAllProducts();

  // @TODO: Just for render all products. Will be refactored.
  // return <ProductList title="All Products" data={latestProducts} limit={20} />;
  return <div>Search page - all products list:</div>;
};

export default SearchPage;
